<h2>ユーザー情報編集画面</h2>
<%= form_with model: @user, url: user_path(@user), local: true do |f| %>
  <%= f.attachment_field :image %>
  <%= f.text_field :name %>
  <%= f.submit '更新', class: "user_info_update_btn" %>
<% end %>